// Include the stdio (Standard Input and Output) C library (A C library is a structured collection of other files)
#include <stdio.h> // The stdio library provides the printf() function

int main(void) {
    printf("Hello, world!");
}